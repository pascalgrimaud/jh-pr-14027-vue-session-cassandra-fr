import axios from 'axios';

import { ICassTestMapstructEntity } from '@/shared/model/cass-test-mapstruct-entity.model';

const baseApiUrl = 'api/cass-test-mapstruct-entities';

export default class CassTestMapstructEntityService {
  public find(id: string): Promise<ICassTestMapstructEntity> {
    return new Promise<ICassTestMapstructEntity>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: string): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: ICassTestMapstructEntity): Promise<ICassTestMapstructEntity> {
    return new Promise<ICassTestMapstructEntity>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: ICassTestMapstructEntity): Promise<ICassTestMapstructEntity> {
    return new Promise<ICassTestMapstructEntity>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
