import axios from 'axios';

import { ICassTestServiceClassEntity } from '@/shared/model/cass-test-service-class-entity.model';

const baseApiUrl = 'api/cass-test-service-class-entities';

export default class CassTestServiceClassEntityService {
  public find(id: string): Promise<ICassTestServiceClassEntity> {
    return new Promise<ICassTestServiceClassEntity>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: string): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: ICassTestServiceClassEntity): Promise<ICassTestServiceClassEntity> {
    return new Promise<ICassTestServiceClassEntity>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: ICassTestServiceClassEntity): Promise<ICassTestServiceClassEntity> {
    return new Promise<ICassTestServiceClassEntity>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
